Again, at <http://phd.martin-krafft.net/wiki/tag/factors> in the lower map, which iterates `link(tag/factors/*)`, the list with the factors is nested within *three* other `<ul>` tags. That seems a bit excessive.