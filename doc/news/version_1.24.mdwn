ikiwiki 1.24 released with these changes:

   * Simplify the data structure returned by rcs\_recentchanges to avoid
     each rcs plugin needing to form complex strings on its own.
   * Fix another destpage issue in the inline directive. Closes: #[385512](http://bugs.debian.org/385512)
     again.